# pdf-flight-schedule-parser
Python pipeline that extracts structured data from schedule PDFs and identifies optimal options based on user-defined preferences. Demonstrates document parsing, dataset construction, and rule-based optimization.
